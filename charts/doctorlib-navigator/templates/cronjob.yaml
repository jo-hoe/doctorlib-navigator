apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "doctorlib-navigator.fullname" . }}
  labels:
    {{- include "doctorlib-navigator.labels" . | nindent 4 }}
spec:
  schedule: {{ .Values.job.schedule | quote }}
  {{- with .Values.job.timeZone }}
  timeZone: {{ . | quote }}
  {{- end }}
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      ttlSecondsAfterFinished: {{ .Values.job.ttlSecondsAfterFinished }}
      template:
        metadata:
          labels:
            {{- include "doctorlib-navigator.selectorLabels" . | nindent 12 }}
        spec:
          restartPolicy: OnFailure
          serviceAccountName: {{ include "doctorlib-navigator.serviceAccountName" . }}
          securityContext:
            {{- toYaml .Values.podSecurityContext | nindent 12 }}
          containers:
            - name: {{ .Chart.Name }}
              image: "{{ .Values.image.repository }}:{{ include "doctorlib-navigator.imageTag" . }}"
              imagePullPolicy: {{ .Values.image.pullPolicy }}
              env:
                - name: CONFIG_PATH
                  value: /run/config/config.yaml
                - name: LOG_LEVEL
                  value: {{ .Values.logLevel | quote }}
                - name: STATE_FILE_PATH
                  value: /run/state/state.json
                {{- if .Values.notification.email.username }}
                - name: SMTP_SECRETS_DIR
                  value: /run/secrets/smtp
                {{- end }}
              securityContext:
                {{- toYaml .Values.securityContext | nindent 16 }}
              resources:
                {{- toYaml .Values.resources | nindent 16 }}
              volumeMounts:
                - name: config
                  mountPath: /run/config
                  readOnly: true
                - name: state
                  mountPath: /run/state
                {{- if .Values.notification.email.username }}
                - name: smtp-secrets
                  mountPath: /run/secrets/smtp
                  readOnly: true
                {{- end }}
          volumes:
            - name: config
              configMap:
                name: {{ include "doctorlib-navigator.fullname" . }}-config
            - name: state
              persistentVolumeClaim:
                claimName: {{ include "doctorlib-navigator.fullname" . }}-state
            {{- if .Values.notification.email.username }}
            - name: smtp-secrets
              secret:
                secretName: {{ include "doctorlib-navigator.fullname" . }}-smtp
            {{- end }}
          {{- with .Values.nodeSelector }}
          nodeSelector:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.affinity }}
          affinity:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.tolerations }}
          tolerations:
            {{- toYaml . | nindent 12 }}
          {{- end }}
