{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "doctorlib-navigator.serviceAccountName" . }}
  labels:
    {{- include "doctorlib-navigator.labels" . | nindent 4 }}
{{- end }}
